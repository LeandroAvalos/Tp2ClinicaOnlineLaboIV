import { InteriorCardDirective } from './interior-card.directive';

describe('InteriorCardDirective', () => {
  it('should create an instance', () => {
    const directive = new InteriorCardDirective();
    expect(directive).toBeTruthy();
  });
});
