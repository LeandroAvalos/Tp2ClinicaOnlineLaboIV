import { DeslizarCardsDirective } from './deslizar-cards.directive';

describe('DeslizarCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new DeslizarCardsDirective();
    expect(directive).toBeTruthy();
  });
});
