import { CambioColorPdfExcelDirective } from './cambio-color-pdf-excel.directive';

describe('CambioColorPdfExcelDirective', () => {
  it('should create an instance', () => {
    const directive = new CambioColorPdfExcelDirective();
    expect(directive).toBeTruthy();
  });
});
